<div id="monday">
  <% if hint.monday_title != "" %>
    <%= hint.monday_title %>
    <div id="monday_thumbnail" style="background-image:url('<%= hint.thumbnail %>')"></div>
  <% else %>
    MONDAY
    <div id="lock"></div>
  <% end %>
</div>

<div id="tuesday">
  <% if hint.tuesday_title != "" %>
    <%= hint.tuesday_title %>
    <div id="tuesday_thumbnail" style="background-image:url('<%= hint.thumbnail %>')"></div>
  <% else %>
    TUESDAY
    <div id="lock"></div>
  <% end %>
</div>

<div id="wednesday">
  <% if hint.wednesday_title != "" %>
    <%= hint.wednesday_title %>
    <div id="wednesday_thumbnail" style="background-image:url('<%= hint.thumbnail %>')"></div>
  <% else %>
    WEDNESDAY
    <div id="lock"></div>
  <% end %>
</div>

<div id="thursday">
  <% if hint.thursday_title != "" %>
    <%= hint.thursday_title %>
    <div id="thursday_thumbnail" style="background-image:url('<%= hint.thumbnail %>')"></div>
  <% else %>
    THURSDAY
    <div id="lock"></div>
  <% end %>
</div>

<div id="friday">
  <% if hint.friday_title != "" %>
    <%= hint.friday_title %>
    <div id="friday_thumbnail" style="background-image:url('<%= hint.thumbnail %>')"></div>
  <% else %>
    FRIDAY
    <div id="lock"></div>
  <% end %>
</div>

<div id="mondays">
  <div id="monday_big" class="big_hint" style="background-image:url('<%= hint.image %>')"></div>
  <div id="monday_side" class="big_hint"><div class="x_white">&times;</div><p><strong><%= hint.monday_title %></strong></p><div class="hint_day"><em>hint posted on Monday</em></div><p><%= album.album_title %> was hidden in an Omaha business <%= album.place_title %>. If you know which one, go there and ask an employee for the final clues.</p></div>
</div>

<div id="tuesdays">
  <div id="tuesday_big" class="big_hint" style="background-image:url('<%= hint.image %>')"></div>
  <div id="tuesday_side" class="big_hint"><div class="x_white">&times;</div><p><strong><%= hint.tuesday_title %></strong></p><div class="hint_day"><em>hint posted on Tuesday</em></div><p><%= album.album_title %> was hidden in an Omaha business <%= album.place_title %>. If you know which one, go there and ask an employee for the final clues.</p></div>
</div>

<div id="wednesdays">
  <div id="wednesday_big" class="big_hint" style="background-image:url('<%= hint.image %>')"></div>
  <div id="wednesday_side" class="big_hint"><div class="x_white">&times;</div><p><strong><%= hint.wednesday_title %></strong></p><div class="hint_day"><em>hint posted on Wednesday</em></div><p><%= album.album_title %> was hidden in an Omaha business <%= album.place_title %>. If you know which one, go there and ask an employee for the final clues.</p></div>
</div>

<div id="thursdays">
  <div id="thursday_big" class="big_hint" style="background-image:url('<%= hint.image %>')"></div>
  <div id="thursday_side" class="big_hint"><div class="x_white">&times;</div><p><strong><%= hint.thursday_title %></strong></p><div class="hint_day"><em>hint posted on Thursday</em></div><p><%= album.album_title %> was hidden in an Omaha business <%= album.place_title %>. If you know which one, go there and ask an employee for the final clues.</p></div>
</div>

<div id="fridays">
  <div id="friday_big" class="big_hint" style="background-image:url('<%= hint.image %>')"></div>
  <div id="friday_side" class="big_hint"><div class="x_white">&times;</div><p><strong><%= hint.friday_title %></strong></p><div class="hint_day"><em>hint posted on Friday</em></div><p><%= album.album_title %> was hidden in an Omaha business <%= album.place_title %>. If you know which one, go there and ask an employee for the final clues.</p></div>
</div>

<% if current_user %>
  <div id="members">
    <div id="member_big" class="big_hint" style="background-image:url('<%= hint.image %>')"></div>
    <div id="member_side"  class="big_hint"><div class="x_white">&times;</div><p><strong><%= hint.member_title %></strong></p><div class="hint_day"><em>hint posted on Sunday</em></div><p><%= album.album_title %> was hidden in an Omaha business <%= album.place_title %>. If you know which one, go there and ask an employee for the final clues. Best of luck.</p></div>
  </div>
  <div id="members_only">▲ SEE MY MEMBERS-ONLY HINT ▲</div>
<% end %>
